{
  "name": "workspace-tui",
  "version": "0.1.0",
  "type": "module",
  "packageManager": "pnpm@10.11.0",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "postinstall": "bash scripts/setup.sh",
    "start": "tsx --tsconfig .config/tsconfig.json src/index.ts",
    "dev": "tsx --tsconfig .config/tsconfig.json src/index.ts",
    "test": "vitest",
    "test:e2e": "cucumber-js",
    "lint": "biome check .",
    "format": "biome format --write .",
    "check": "biome check . && tsc -p .config/tsconfig.json --noEmit && vitest run"
  },
  "dependencies": {
    "dotenv": "16.5.0",
    "google-auth-library": "9.15.1",
    "googleapis": "148.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.14",
    "@types/node": "24.0.3",
    "tsx": "4.21.0",
    "typescript": "5.8.3",
    "vitest": "3.1.3"
  },
  "engines": {
    "node": ">=24.0.0",
    "pnpm": ">=10.0.0"
  },
  "tools": {
    "required": {
      "gitleaks": "8.18.0",
      "cloc": "1.90.0"
    },
    "optional": {
      "shellcheck": "0.9.0",
      "ripgrep": "13.0.0"
    }
  }
}
